---
title: "A Step Forward Towards Trustworthy Risk-Aware Facial Retrieval"
authors:
  - name: Muhammad Emmad Siddiqui
    url: https://github.com/MuhammadEmmadSiddiqui
    institution: FAST NUCES
  - name: Muhammad Rafi
    url: https://scholar.google.com/citations?user=AIuzS3sAAAAJ&hl=en
    institution: FAST NUCES
conference: Accepted at ICPR 2026, Lyon, France
links:
  - name: Paper
    url: "https://arxiv.org/pdf/2607.16279"
    icon: ri:file-pdf-2-line
  - name: Code
    url: https://github.com/MuhammadEmmadSiddiqui/RA-FR
    icon: ri:github-line

theme: light
favicon: favicon.svg
description: "RA-FR: A trustworthy risk-aware facial retrieval framework integrating uncertainty quantification with face retrieval via Bayesian Transfer Learning and Monte Carlo Dropout."
---

import HighlightedSection from "./components/HighlightedSection.astro";
import SmallCaps from "./components/SmallCaps.astro";
import Figure from "./components/Figure.astro";
import Picture from "./components/Picture.astro";
import TwoColumns from "./components/TwoColumns.astro";
import Wide from "./components/Wide.astro";

import calibrationPlot from "./assets/RA-FR (1).png";
import resultsFigure from "./assets/RA-FR (2).png";

<HighlightedSection>

## Abstract

Facial image retrieval in unconstrained surveillance environments is a high-stakes challenge where missing a subject of interest—a single false negative—is simply not an option. Despite near-p[...]

</HighlightedSection>

## Motivation

Face retrieval is increasingly deployed in security, surveillance, and identity verification. Yet most systems treat every prediction with equal confidence, making them vulnerable in safety-critic[...]

## Method

### Model Architectures

We evaluate three architectures that pair a visual backbone with a pooling strategy suited to retrieval:

| Architecture | Backbone | Pooling | Role |
| :--- | :---: | :---: | :---: |
| R50-GeM | ResNet-50 | Generalized Mean (GeM) | Baseline |
| ViTB-DN-GeM | ViT-Base + DINO | GeM | Proposed |
| ViTB-DN-GGeM | ViT-Base + DINO | Group GeM | Proposed (Main) |

The **DINO**-pretrained ViT-Base backbone provides strong spatial representations learned through self-supervised distillation. **Group GeM** extends standard GeM by applying generalized mean pool[...]

### Uncertainty Estimation

We employ two complementary uncertainty estimation approaches:

- **Bayesian Transfer Learning (BTL):** Places a Bayesian last layer on top of the frozen or fine-tuned feature extractor, enabling closed-form posterior inference over retrieval scores without co[...]
- **Monte Carlo Dropout (MCD):** Activates dropout at test time and performs $T$ stochastic forward passes. The variance across the resulting embedding distributions serves as the uncertainty esti[...]

### Risk-Aware Retrieval Pipeline

The RA-FR pipeline augments standard retrieval with an uncertainty-driven decision layer:

1. **Embed** the query face using the trained backbone + pooling.
2. **Retrieve** nearest neighbors from the gallery via cosine similarity.
3. **Estimate uncertainty** of the retrieval using BTL or MCD.
4. **Flag** retrievals whose uncertainty exceeds a calibrated threshold, enabling operators to request manual verification instead of trusting an unreliable match.

## Results

### Calibration & Uncertainty Analysis

The figure below shows reliability diagrams (Recall@1 vs. binned uncertainty) and uncertainty density distributions across both datasets. A well-calibrated model should track the diagonal (dashed [...]

<Wide>
  <Figure>
    <Picture slot="figure" src={calibrationPlot} alt="Reliability diagrams and uncertainty density distributions for BTL and MCD on IMFDB and SCFace datasets, comparing ViTB-DN-GGeM, ViTB-DN-GeM, [...]
    <Fragment slot="caption">**Calibration analysis.** Top row: reliability diagrams (Recall@1 vs. uncertainty) with ECE values. Bottom row: uncertainty density distributions. Columns compare BTL [...]
  </Figure>
</Wide>

Key findings from the calibration analysis:

- **ViTB-DN-GeM achieves the best calibration** on IMFDB with BTL (ECE = 0.245) and MCD (ECE = 0.059), closely tracking the ideal diagonal.
- **R50-GeM struggles on SCFace**, where ECE degrades to 0.485 (BTL) and 0.482 (MCD), reflecting poor generalization to surveillance conditions.
- **MCD produces tighter uncertainty distributions** compared to BTL, especially for the ViT-based architectures.
- The proposed architectures exhibit **higher density at lower uncertainty values** on IMFDB, indicating more confident and better-calibrated in-domain retrieval.

### Cross-Domain Generalization

<Wide>
  <Figure>
    <Picture slot="figure" src={resultsFigure} alt="Additional results showing cross-domain evaluation and retrieval performance comparison between architectures on IMFDB and SCFace." />
    <Fragment slot="caption">**Cross-domain evaluation.** Performance comparison of the three architectures across IMFDB (in-domain) and SCFace (cross-domain) settings, demonstrating the robustne[...]
  </Figure>
</Wide>

The cross-domain evaluation from IMFDB → SCFace simulates a realistic deployment scenario, where training data comes from controlled movie scenes but the system must handle low-resolution surve[...]

## Key Takeaways

- **Uncertainty matters for trust.** Integrating uncertainty quantification into face retrieval transforms a black-box system into one that can communicate reliability, which is essential for saf[...]
- **ViT + DINO features excel at calibration.** Self-supervised ViT representations produce better-calibrated uncertainty estimates than supervised ResNet features, especially under domain shift.
- **MCD and BTL are complementary.** MCD tends to produce tighter, better-separated uncertainty distributions, while BTL provides computationally cheaper estimates with competitive calibration.
- **Group GeM improves retrieval capacity.** The ViTB-DN-GGeM architecture achieves the strongest raw retrieval performance, extending the standard GeM with group-wise pooling over ViT features.

## BibTeX Citation

```bibtex
@misc{siddiqui2025rafr,
  author    = {Siddiqui, Muhammad Emmad and Rafi, Muhammad},
  title     = {A Step Forward Towards Trustworthy Risk-Aware Facial Retrieval},
  year      = {2025},
  publisher = {GitHub},
  howpublished = {\url{https://github.com/MuhammadEmmadSiddiqui/RA-FR}},
}
```
